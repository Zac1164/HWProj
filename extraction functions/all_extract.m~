function all_extract(file,folder)

slant_extract(file,folder);
#skew_extract(file,folder);
#asc_vs_desc_extract(file,folder);
#curvature_extract(file,folder);
#curvature_direction_extract(file,folder);
#slant_energy_extract(file,folder);
#entropy_extract(file,folder);

endfunction
